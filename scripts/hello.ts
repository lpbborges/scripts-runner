/**
 * Greets a user
 * @param name The name of the user
 * @param age The age of the user
 */
export function main(name: string, age: number) {
  console.log(`Hello ${name}, you are ${age} years old`);
}
